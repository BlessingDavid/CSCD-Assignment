#include <iostream>
  using namespace std;
  bool is_prme (int num {
  int f_count = 0;
  for (int i = 1; i <=num; i++) {
  if (num % i == 0) {
  f_ count++;
      } 
}
if (f_count == 2)  {
        return true;
    } else {
        return false:
    }
}
